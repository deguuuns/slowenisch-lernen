'use client'

import { useState } from 'react'
import { ChevronRight } from 'lucide-react'
import AudioButton from '@/components/AudioButton'
import ExerciseDeck,{ExerciseResultMeta} from '@/components/ExerciseDeck'
import { exercises,lessons,vocabulary } from '@/data/seed'
import { buildFinalReview,buildLearningBlocks,lessonProgress } from '@/lib/learning-flow'
import { Exercise,UserProgress } from '@/types'

type Stage='intro'|'learn'|'practice'|'grammar'|'final'|'done'
function blockSize(progress:UserProgress){return progress.preferences.pace==='ruhig'?2:3}
export default function LessonFlow({lessonId,progress,setProgress,onExerciseResult,onFinish}:{lessonId:number;progress:UserProgress;setProgress:(f:any)=>void;onExerciseResult:(e:Exercise,c:boolean,meta:ExerciseResultMeta)=>void;onFinish:()=>void}){
 const lesson=lessons.find(l=>l.id===lessonId)!,[blocks]=useState(()=>buildLearningBlocks(lessonId,vocabulary,exercises,progress.introducedWords,blockSize(progress))),[stage,setStage]=useState<Stage>(blocks.length?'intro':'final'),[blockIndex,setBlockIndex]=useState(0),[wordIndex,setWordIndex]=useState(0),block=blocks[blockIndex],finalReview=buildFinalReview(lessonId,exercises),percent=stage==='done'?100:blocks.length?lessonProgress(blockIndex,blocks.length,stage==='practice'?1:0,2):75
 function markWord(id:string){setProgress((p:UserProgress)=>({...p,introducedWords:Array.from(new Set([...p.introducedWords,id])),updatedAt:Date.now()}))}
 function nextWord(){if(!block)return;markWord(block.words[wordIndex].id);if(wordIndex<block.words.length-1)setWordIndex(wordIndex+1);else{setWordIndex(0);setStage('practice')}}
 function nextBlock(){if(blockIndex<blocks.length-1){setBlockIndex(blockIndex+1);setWordIndex(0);setStage('learn')}else setStage('grammar')}
 if(!lesson)return null
 return <div className="w-full max-w-full space-y-5 overflow-x-hidden"><div className="card w-full max-w-full"><div className="flex items-center justify-between gap-3"><div className="text-sm font-bold text-lime-700">Lektion {lesson.id}</div><div className="text-sm font-bold text-slate-500">{percent}%</div></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full bg-lime-400 transition-all" style={{width:`${percent}%`}}/></div><h2 className="mt-4 break-words text-3xl font-black">{lesson.title}</h2><p className="mt-2 break-words text-slate-600">{lesson.subtitle}</p></div>{stage==='intro'&&<div className="card w-full max-w-full"><h3 className="text-xl font-black">Heute lernst du in kleinen Portionen</h3><p className="mt-2 text-slate-600">{blockSize(progress)} neue Wörter, danach sofort passende Aufgaben. Bereits eingeführte Wörter werden nicht erneut vorgestellt.</p><button onClick={()=>setStage('learn')} className="btn-primary mt-5">Los geht’s <ChevronRight size={18}/></button></div>}{stage==='learn'&&block&&<div className="card w-full max-w-full overflow-hidden"><div className="break-words text-sm font-bold text-lime-700">Lernblock {blockIndex+1} von {blocks.length} · Wort {wordIndex+1} von {block.words.length}</div><div className="mt-5 grid min-w-0 grid-cols-1 gap-4 lg:grid-cols-[minmax(0,1fr)_minmax(0,1fr)]"><div className="min-w-0"><AudioButton text={block.words[wordIndex].sl}/></div><div className="min-w-0"><div className="break-words text-3xl font-black">{block.words[wordIndex].sl}</div><div className="mt-1 break-words text-lg text-slate-600">{block.words[wordIndex].de}</div><div className="mt-4 max-w-full break-words rounded-2xl bg-slate-50 p-4"><b>{block.words[wordIndex].example}</b><div className="mt-1 text-sm text-slate-500">{block.words[wordIndex].exampleDe}</div></div></div></div><button onClick={nextWord} className="btn-primary mt-5 w-full justify-center">{wordIndex===block.words.length-1?'Jetzt anwenden':'Nächstes Wort'} <ChevronRight size={18}/></button></div>}{stage==='practice'&&block&&<ExerciseDeck exercises={block.exercises} onResult={onExerciseResult} onComplete={nextBlock}/>} {stage==='grammar'&&<div className="card w-full max-w-full"><div className="text-sm font-bold text-lime-700">Jetzt ergibt die Grammatik Sinn</div><h3 className="mt-1 text-2xl font-black">{lesson.grammar.title}</h3><p className="mt-3 text-slate-600">{lesson.grammar.body}</p><div className="mt-4 space-y-2">{lesson.grammar.examples.map(e=><div key={e} className="rounded-2xl bg-lime-50 p-3 font-semibold">{e}</div>)}</div><button onClick={()=>setStage('final')} className="btn-primary mt-5">Zum Abschlusstest <ChevronRight size={18}/></button></div>}{stage==='final'&&<><div className="card w-full max-w-full"><div className="text-sm font-bold text-lime-700">Gemischter Abschluss</div><h3 className="mt-1 text-2xl font-black">Zeig, was hängen geblieben ist</h3><p className="mt-2 text-slate-600">Sicher wird ein Wort erst nach aktiver Anwendung und späteren Wiederholungen.</p></div><ExerciseDeck exercises={finalReview} onResult={onExerciseResult} onComplete={()=>setStage('done')}/></>}{stage==='done'&&<div className="card w-full max-w-full text-center"><div className="text-sm font-bold text-lime-700">100 %</div><h3 className="mt-2 text-3xl font-black">Lektion geschafft</h3><p className="mt-2 text-slate-600">Die Lektion ist abgeschlossen; einzelne Wörter dürfen trotzdem noch im Status „lernen“ sein.</p><button onClick={onFinish} className="btn-primary mt-5 justify-center">Lektion abschließen</button></div>}</div>
}
