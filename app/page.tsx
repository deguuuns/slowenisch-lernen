'use client'

import { Dispatch, SetStateAction, useEffect, useRef, useState } from 'react'
import { BookOpen, Brain, ChevronRight, Flame, GraduationCap, Headphones, Home, LucideIcon, Mic2, RotateCcw, Sparkles, Trophy } from 'lucide-react'
import AccountMenu from '@/components/AccountMenu'
import AdaptiveSessionInfo from '@/components/AdaptiveSessionInfo'
import DailyTrainingSession from '@/components/DailyTrainingSession'
import ExerciseDeck, { ExerciseResultMeta } from '@/components/ExerciseDeck'
import LessonFlow from '@/components/LessonFlow'
import Onboarding from '@/components/Onboarding'
import ProgressProfile from '@/components/ProgressProfile'
import SpeechPractice, { SpeechResultMeta } from '@/components/SpeechPractice'
import TutorChat from '@/components/TutorChat'
import VocabWorkspace from '@/components/VocabWorkspace'
import { exercises, lessons, vocabulary } from '@/data/curriculum'
import { CloudSession, ensureProfile, loadCloudProgress, loadSession, mergeProgress, saveCloudProgress } from '@/lib/cloud-sync'
import { buildDailyTrainingPlan, dailyTrainingLabel } from '@/lib/daily-training'
import { createExerciseSession } from '@/lib/exercise-session'
import { reconcileVocabularyProgress } from '@/lib/learner-status'
import { buildSessionPlan, exercisesForPlan } from '@/lib/session-planner'
import { clearGuestProgress, defaultProgress, hasMeaningfulProgress, isActiveProduction, loadProgress, queueTransfers, recordAttempt, registerMistake, saveProgress, scheduleExerciseReviews, updateMastery, updateSkillMastery } from '@/lib/storage'
import { Exercise, LearnerPreferences, UserProgress } from '@/types'

type Tab='home'|'today'|'lesson'|'review'|'speak'|'vocab'|'progress'
type ProgressSetter=Dispatch<SetStateAction<UserProgress>>
type NavItem={id:Tab;label:string;icon:LucideIcon}
const nav:NavItem[]=[{id:'home',label:'Start',icon:Home},{id:'lesson',label:'Lektionen',icon:BookOpen},{id:'review',label:'Wiederholen',icon:RotateCcw},{id:'speak',label:'Sprechen',icon:Mic2},{id:'vocab',label:'Vokabeln',icon:Brain},{id:'progress',label:'Fortschritt',icon:Trophy}]
const errorText=(error:unknown)=>error instanceof Error?error.message:''

export default function Page(){
  const [tab,setTab]=useState<Tab>('home'),[progress,setProgress]=useState<UserProgress>(defaultProgress),[lessonId,setLessonId]=useState(1),[session,setSession]=useState<CloudSession|null>(null),[profileId,setProfileId]=useState<string|null>(null),[progressScope,setProgressScope]=useState<string|null>(null),[syncState,setSyncState]=useState('nur lokal')
  const hydrated=useRef(false),syncTimer=useRef<ReturnType<typeof setTimeout>|null>(null)
  useEffect(()=>{const saved=loadSession(),scope=saved?.user.id||null;setSession(saved);setProgressScope(scope);setProgress(loadProgress(scope));hydrated.current=true},[])
  useEffect(()=>{if(hydrated.current)saveProgress(progress,progressScope)},[progress,progressScope])
  useEffect(()=>{if(!session){setProfileId(null);return}let cancelled=false;(async()=>{try{setSyncState('verbinde …');const profile=await ensureProfile(session);if(cancelled)return;setProfileId(profile.id);const userLocal=loadProgress(session.user.id),cloud=await loadCloudProgress(session,profile.id);if(cancelled)return;let merged:UserProgress;if(cloud)merged=mergeProgress(userLocal,cloud);else if(hasMeaningfulProgress(userLocal))merged=userLocal;else{const guest=loadProgress(null);merged=hasMeaningfulProgress(guest)?mergeProgress(userLocal,guest):userLocal;if(hasMeaningfulProgress(guest))clearGuestProgress()}setProgressScope(session.user.id);setProgress(merged);saveProgress(merged,session.user.id);setSyncState(`synchronisiert · ${new Date().toLocaleTimeString('de-DE',{hour:'2-digit',minute:'2-digit'})}`)}catch(error){console.error('Cloud sync connection failed');setSyncState(errorText(error).includes('abgelaufen')?'Anmeldung erforderlich':'offline – lokal gespeichert')}})();return()=>{cancelled=true}},[session])
  useEffect(()=>{if(!session||!profileId||progressScope!==session.user.id||!hydrated.current)return;if(syncTimer.current)clearTimeout(syncTimer.current);syncTimer.current=setTimeout(async()=>{try{setSyncState('synchronisiere …');await saveCloudProgress(session,profileId,progress);setSyncState(`synchronisiert · ${new Date().toLocaleTimeString('de-DE',{hour:'2-digit',minute:'2-digit'})}`)}catch{console.error('Cloud sync save failed');setSyncState('offline – lokal gespeichert')}},1200);return()=>{if(syncTimer.current)clearTimeout(syncTimer.current)}},[progress,session,profileId,progressScope])
  const activeLesson=Math.min(lessons.length,Math.max(1,[...progress.completedLessons,0].sort((a,b)=>b-a)[0]+1))
  function handleSession(next:CloudSession|null){if(!next){if(progressScope)saveProgress(progress,progressScope);setProgressScope(null);setProgress(loadProgress(null));setProfileId(null);setSyncState('nur lokal')}setSession(next)}
  function finishLesson(id:number){setProgress(previous=>({...previous,completedLessons:Array.from(new Set([...previous.completedLessons,id])),updatedAt:Date.now()}));setTab('progress')}
  function setPreferences(preferences:LearnerPreferences){const now=Date.now();setProgress(previous=>({...previous,preferences,preferencesUpdatedAt:now,updatedAt:now}))}
  return <main className="min-h-screen min-w-0 max-w-full overflow-x-hidden pb-[calc(6.5rem+env(safe-area-inset-bottom))] md:pb-8"><div className="mx-auto w-full min-w-0 max-w-6xl px-3 py-5 sm:px-4 md:px-8 md:py-8"><Header streak={progress.streak} session={session} syncState={syncState} preferences={progress.preferences} onSession={handleSession} onPreferences={setPreferences}/><div className="mt-6 grid min-w-0 gap-6 md:grid-cols-[220px_minmax(0,1fr)]"><aside className="hidden md:block"><DesktopNav tab={tab} setTab={setTab}/></aside><section className="min-w-0 max-w-full">{tab==='home'&&<HomeView progress={progress} activeLesson={activeLesson} onContinue={()=>setTab('today')} onGo={setTab}/>} {tab==='today'&&<DailyTrainingSession progress={progress} setProgress={setProgress} exercises={exercises} vocabulary={vocabulary} activeLesson={activeLesson} onExerciseResult={(exercise,correct,meta)=>handleExercise(exercise,correct,meta,setProgress)} onFinish={()=>setTab('progress')}/>} {tab==='lesson'&&<LessonArea lessonId={lessonId} setLessonId={setLessonId} progress={progress} setProgress={setProgress} finishLesson={finishLesson}/>} {tab==='review'&&<ReviewView progress={progress} activeLesson={activeLesson} setProgress={setProgress}/>} {tab==='speak'&&<SpeakView setProgress={setProgress}/>} {tab==='vocab'&&<VocabWorkspace vocabulary={vocabulary} progress={progress} onResult={(exercise,correct,meta)=>handleExercise(exercise,correct,meta,setProgress)}/>} {tab==='progress'&&<ProgressView progress={progress} cloud={Boolean(session)} syncState={syncState}/>}</section></div></div><MobileNav tab={tab} setTab={setTab}/><Onboarding preferences={progress.preferences} onSave={setPreferences}/></main>
}

function Header({streak,session,syncState,preferences,onSession,onPreferences}:{streak:number;session:CloudSession|null;syncState:string;preferences:LearnerPreferences;onSession:(session:CloudSession|null)=>void;onPreferences:(preferences:LearnerPreferences)=>void}){return <header className="flex min-w-0 items-start justify-between gap-2 sm:items-center sm:gap-3"><div className="min-w-0 flex-1"><div className="text-xs font-bold uppercase tracking-[0.24em] text-lime-700">Slovensko</div><h1 className="min-w-0 break-words text-xl font-black tracking-tight sm:text-2xl [overflow-wrap:anywhere]">Slowenisch, das du wirklich sprichst.</h1></div><div className="flex shrink-0 items-center gap-2"><div className="hidden items-center gap-2 rounded-full bg-white px-3 py-2 shadow-soft sm:flex"><Flame size={18}/><span className="font-bold">{streak}</span><span className="text-sm text-slate-500">Tage</span></div><AccountMenu session={session} syncState={syncState} preferences={preferences} onSession={onSession} onPreferences={onPreferences}/></div></header>}
function DesktopNav({tab,setTab}:{tab:Tab;setTab:(tab:Tab)=>void}){return <nav className="sticky top-6 space-y-1 rounded-3xl bg-white p-3 shadow-soft">{nav.map(item=><button key={item.id} onClick={()=>setTab(item.id)} className={`flex w-full items-center gap-3 rounded-2xl px-3 py-3 text-left font-semibold ${tab===item.id?'bg-lime-200 text-slate-950':'text-slate-600 hover:bg-slate-50'}`}><item.icon size={19}/>{item.label}</button>)}</nav>}
function MobileNav({tab,setTab}:{tab:Tab;setTab:(tab:Tab)=>void}){return <nav className="fixed inset-x-0 bottom-0 z-50 grid min-w-0 grid-cols-6 border-t border-slate-200 bg-white/95 px-1 pt-2 pb-[max(0.5rem,env(safe-area-inset-bottom))] backdrop-blur md:hidden">{nav.map(item=><button key={item.id} onClick={()=>setTab(item.id)} className={`flex min-h-12 min-w-0 flex-col items-center justify-center gap-1 px-0.5 text-[9px] font-semibold sm:text-[10px] ${tab===item.id?'text-lime-700':'text-slate-500'}`} aria-label={item.label}><item.icon size={19}/><span className="max-w-full truncate">{item.label}</span></button>)}</nav>}
function Metric({icon:Icon,value,label}:{icon:LucideIcon;value:string;label:string}){return <div className="card min-w-0"><Icon className="mb-3" size={20}/><div className="break-words text-2xl font-black">{value}</div><div className="break-words text-sm text-slate-500">{label}</div></div>}
function Quick({icon:Icon,label,onClick}:{icon:LucideIcon;label:string;onClick:()=>void}){return <button onClick={onClick} className="card flex min-h-28 min-w-0 flex-col items-start justify-between text-left"><Icon/><span className="break-words font-bold">{label}</span></button>}
function HomeView({progress,activeLesson,onContinue,onGo}:{progress:UserProgress;activeLesson:number;onContinue:()=>void;onGo:(tab:Tab)=>void}){const due=progress.reviews.filter(review=>review.dueAt<=Date.now()).length,weak=Object.values(progress.mastery||{}).filter(item=>item.attempts>=2&&item.score<.58).length,plan=buildDailyTrainingPlan(progress,exercises,activeLesson);return <div className="min-w-0 space-y-6"><div className="min-w-0 overflow-hidden rounded-[2rem] bg-slate-950 p-5 text-white shadow-soft sm:p-6 md:p-8"><div className="min-w-0 max-w-2xl"><div className="mb-2 text-sm font-semibold text-lime-300">Dein adaptiver Lernplan</div><h2 className="break-words text-2xl font-black sm:text-3xl md:text-5xl [overflow-wrap:anywhere]">Heute genau das, <span className="text-lime-300">was dir am meisten bringt.</span></h2><p className="mt-4 max-w-xl break-words text-slate-300">Wiederholen, neuer Stoff, Hören und Sprechen laufen jetzt als eine zusammenhängende Sitzung statt als einzelne Menüs.</p><button onClick={onContinue} className="mt-6 inline-flex min-h-11 items-center gap-2 whitespace-normal rounded-2xl bg-lime-300 px-5 py-3 font-black text-slate-950">Tagestraining starten <ChevronRight/></button></div></div><div className="card min-w-0 border-2 border-lime-200"><div className="flex items-center gap-2 text-sm font-bold text-lime-700"><Sparkles size={18}/>Heute geplant</div><h3 className="mt-2 break-words text-2xl font-black">{dailyTrainingLabel(plan)}</h3><p className="mt-2 break-words text-slate-600 dark:text-slate-300">{plan.blocks.map(block=>block.title).join(' → ')}</p><AdaptiveSessionInfo goalMinutes={plan.goalMinutes} recommendedMinutes={plan.recommendedMinutes} loadLevel={plan.loadLevel} reason={plan.loadReason}/><div className="mt-4 break-words text-xs font-semibold uppercase tracking-wide text-slate-400">{plan.dueCount?`${plan.dueCount} fällig · `:''}{plan.primaryWeakness?`Schwerpunkt ${plan.primaryWeakness.replace(/^\w+:/,'')} · `:''}adaptiv aus Tagesziel {plan.goalMinutes} min</div></div><div className="grid min-w-0 gap-3 sm:grid-cols-4"><Metric icon={Brain} value={`${progress.wordsLearned.length} / ${vocabulary.length}`} label="Gelernter Wortschatz"/><Metric icon={RotateCcw} value={String(due)} label="Heute fällig"/><Metric icon={Sparkles} value={String(weak)} label="Schwache Bereiche"/><Metric icon={GraduationCap} value={`${progress.completedLessons.length} / ${lessons.length}`} label="Lektionen"/></div><div className="grid min-w-0 grid-cols-2 gap-3 sm:grid-cols-4"><Quick icon={RotateCcw} label="Wiederholen" onClick={()=>onGo('review')}/><Quick icon={Mic2} label="Sprechen" onClick={()=>onGo('speak')}/><Quick icon={Brain} label="Vokabeln" onClick={()=>onGo('vocab')}/><Quick icon={Trophy} label="Fortschritt" onClick={()=>onGo('progress')}/></div></div>}
function LessonArea({lessonId,setLessonId,progress,setProgress,finishLesson}:{lessonId:number;setLessonId:(id:number)=>void;progress:UserProgress;setProgress:ProgressSetter;finishLesson:(id:number)=>void}){return <div className="min-w-0 space-y-4"><div className="flex max-w-full gap-2 overflow-x-auto pb-1">{lessons.map(lesson=><button key={lesson.id} onClick={()=>setLessonId(lesson.id)} className={`min-h-11 shrink-0 rounded-full px-4 py-2 text-sm font-bold ${lessonId===lesson.id?'bg-slate-950 text-white':'bg-white text-slate-600'}`}>Lektion {lesson.id}{progress.completedLessons.includes(lesson.id)?' ✓':''}</button>)}</div><LessonFlow key={lessonId} lessonId={lessonId} progress={progress} setProgress={setProgress} onExerciseResult={(exercise,correct,meta)=>handleExercise(exercise,correct,meta,setProgress)} onFinish={()=>finishLesson(lessonId)}/></div>}
function ReviewView({progress,activeLesson,setProgress}:{progress:UserProgress;activeLesson:number;setProgress:ProgressSetter}){const [plan]=useState(()=>buildSessionPlan(progress,exercises,activeLesson)),[reviewSession]=useState(()=>createExerciseSession('review',exercisesForPlan(buildSessionPlan(progress,exercises,activeLesson),exercises),`review:${activeLesson}:${Date.now()}`));return <div className="min-w-0 space-y-5"><div className="card min-w-0"><div className="text-sm font-bold text-lime-700">Karteikasten</div><h2 className="mt-1 break-words text-2xl font-black sm:text-3xl">Nur wirklich fällige Karten</h2><p className="mt-2 break-words text-slate-600">{plan.total?`${plan.total} Lernziel${plan.total===1?'':'e'} sind jetzt fällig.`:'Aktuell ist keine reguläre Karte fällig. Sichere Inhalte bleiben bis zu ihrem Termin im Kasten.'}</p></div>{reviewSession.exercises.length?<ExerciseDeck key={reviewSession.sessionId} session={reviewSession} onResult={(exercise,correct,meta)=>handleExercise(exercise,correct,meta,setProgress)}/>:<div className="card min-w-0"><b>Nichts zu wiederholen.</b><p className="mt-2 text-sm text-slate-600">Du kannst stattdessen mit neuem Stoff weitermachen.</p></div>}</div>}
function SpeakView({setProgress}:{setProgress:ProgressSetter}){const [index,setIndex]=useState(0),prompts=[['Kje si zdaj?','Zdaj sem v Sloveniji.'],['Od kod si?','Sem iz Nemčije.'],['Kje živiš?','Živim v Nemčiji.'],['Kaj piješ?','Pijem vodo.'],['Kdaj greš spat?','Grem spat ob desetih.']];function result(correct:boolean,_actual:string,meta:SpeechResultMeta){setProgress(previous=>{const hints=meta.replays+(meta.usedSlowAudio?1:0),spoken=meta.inputMode==='speech';let mastery=updateSkillMastery(previous.mastery||{},spoken?'speaking:spoken-response':'production:typed-fallback',correct,meta.responseMs,hints);mastery=updateSkillMastery(mastery,spoken?'speaking':'production',correct,meta.responseMs,hints);return {...previous,speakingMinutes:spoken?+(previous.speakingMinutes+.2).toFixed(1):previous.speakingMinutes,mastery,updatedAt:Date.now()}});if(correct)setTimeout(()=>setIndex(value=>(value+1)%prompts.length),900)}return <div className="min-w-0 space-y-5"><div className="card min-w-0"><h2 className="break-words text-2xl font-black sm:text-3xl">Sprechen</h2><p className="mt-2 break-words text-slate-600">Übe gezielte Sprechaufgaben oder führe darunter ein fortlaufendes Gespräch mit dem Tutor. Getippte Antworten zählen als Produktionsübung, nicht als Sprechleistung.</p></div><SpeechPractice prompt={prompts[index][0]} expected={prompts[index][1]} onResult={result}/><TutorChat/></div>}
function ProgressView({progress,cloud,syncState}:{progress:UserProgress;cloud:boolean;syncState:string}){const top=[...progress.mistakes].sort((a,b)=>b.count-a.count).slice(0,5),weak=Object.values(progress.mastery||{}).filter(item=>item.attempts>=2).sort((a,b)=>a.score-b.score).slice(0,5),recent=progress.recentAttempts||[],average=recent.length?Math.round(recent.slice(-20).reduce((sum,item)=>sum+item.responseMs,0)/Math.min(20,recent.length)/1000):0,spoken=Math.round((progress.mastery?.['skill:speaking:spoken-response']?.score||0)*100),listening=Math.round((progress.mastery?.['skill:listening']?.score||0)*100),skillRows=[['Hören · Wörter','skill:listening:word'],['Hören · Sätze','skill:listening:sentence'],['Hören · Dialoge','skill:listening:dialogue'],['Hören · Geschichten','skill:listening:story'],['Sprechen · echte Sprache','skill:speaking:spoken-response'],['Produktion · getippt','skill:production:typed-fallback']] as const;return <div className="min-w-0 space-y-5"><div className="card min-w-0"><h2 className="break-words text-2xl font-black sm:text-3xl">Fortschritt</h2><p className="mt-2 break-words text-slate-600">{cloud?`Cloud-Sync: ${syncState}. Lokale Offline-Kopie bleibt erhalten.`:'Dein Fortschritt ist lokal gespeichert. Über „Konto“ kannst du Geräte-Synchronisation aktivieren.'}</p></div><ProgressProfile progress={progress} totalVocabulary={vocabulary.length} totalLessons={lessons.length}/><div className="grid min-w-0 gap-3 sm:grid-cols-2 lg:grid-cols-4"><Metric icon={Brain} value={`${progress.wordsLearned.length}`} label="gelernte Wörter"/><Metric icon={Trophy} value={`${progress.secureWords.length}`} label="sichere Wörter"/><Metric icon={Mic2} value={spoken?`${spoken} %`:'–'} label="echte Sprech-Mastery"/><Metric icon={Headphones} value={listening?`${listening} %`:(average?`${average} s`:'–')} label={listening?'Hör-Mastery':'Ø Antwortzeit'}/></div><div className="card min-w-0"><h3 className="break-words text-xl font-black">Hören & Sprechen im Detail</h3><p className="mt-2 text-sm text-slate-500">Die App trennt Hörstufen und echte Spracheingabe, damit Tippen deine Sprech-Mastery nicht künstlich erhöht.</p><div className="mt-4 grid gap-2 sm:grid-cols-2">{skillRows.map(([label,key])=>{const item=progress.mastery?.[key];return <div key={key} className="flex items-center justify-between gap-3 rounded-2xl bg-slate-50 p-3"><span className="text-sm font-semibold">{label}</span><b>{item&&item.attempts?`${Math.round(item.score*100)} %`:'–'}</b></div>})}</div></div><div className="grid min-w-0 gap-4 lg:grid-cols-2"><div className="card min-w-0"><h3 className="break-words text-xl font-black">Häufigste Fehler</h3>{top.length?<div className="mt-4 space-y-2">{top.map(item=><div key={item.key} className="flex min-w-0 flex-wrap justify-between gap-2 rounded-2xl bg-amber-50 p-3"><span className="min-w-0 flex-1 break-words [overflow-wrap:anywhere]">{exercises.find(exercise=>exercise.id===item.key)?.prompt??item.key}</span><b className="shrink-0">{item.count}×</b></div>)}</div>:<p className="mt-3 text-slate-500">Noch keine Fehler gespeichert.</p>}</div><div className="card min-w-0"><h3 className="break-words text-xl font-black">Adaptive Schwerpunkte</h3>{weak.length?<div className="mt-4 space-y-2">{weak.map(item=><div key={item.key} className="flex min-w-0 flex-wrap justify-between gap-2 rounded-2xl bg-lime-50 p-3"><span className="min-w-0 flex-1 break-words [overflow-wrap:anywhere]">{item.key.replace(/^(vocab|grammar|skill|verb):/,'')}</span><b className="shrink-0">{Math.round(item.score*100)} %</b></div>)}</div>:<p className="mt-3 text-slate-500">Nach einigen Übungen erkennt die App hier deine schwächeren Bereiche.</p>}</div></div></div>}
function handleExercise(exercise:Exercise,correct:boolean,meta:{responseMs:number;hintsUsed:number},setProgress:ProgressSetter){setProgress(previous=>{const attemptCount=(previous.recentAttempts?.length||0)+1,now=Date.now(),reviews=scheduleExerciseReviews(previous.reviews,exercise,correct,now),mastery=updateMastery(previous.mastery||{},exercise,correct,meta.responseMs,meta.hintsUsed),attempt={exerciseId:exercise.id,correct,responseMs:meta.responseMs,hintsUsed:meta.hintsUsed,occurredAt:now,vocabularyIds:exercise.vocabularyIds||[],grammarRuleIds:exercise.grammarRuleIds||[],skillTargets:exercise.skillTargets||[],activeProduction:isActiveProduction(exercise)},next:UserProgress={...previous,reviews,mistakes:correct?previous.mistakes:registerMistake(previous.mistakes,exercise.id),mastery,recentAttempts:recordAttempt(previous.recentAttempts||[],attempt),transferQueue:queueTransfers(previous.transferQueue||[],exercise,correct,attemptCount),updatedAt:now};return reconcileVocabularyProgress(next,exercise.vocabularyIds||[])})}
